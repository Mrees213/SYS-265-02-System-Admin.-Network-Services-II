sudo groupadd docker
This creates a docker group using the sudo code.
sudo usermod -aG docker morgan

cat /etc/group | grep docker
This displays lines from the group and docker file. 
systemctl restart docker
This is restart the docker. 
newgrp docker
This command changes the group id during a login session
docker run hello-world
creates a new container from the image we provided to it
